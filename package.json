{
  "name": "electron-remote-control",
  "productName": "Remote Control Package",
  "version": "1.1.45",
  "description": "An advanced, but easy-to-use Remote Desktop Application Program which has cross-platform support",
  "main": "package.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildInstallable": "node scripts/executeScript.js --buildInstallable",
    "openInstallable": "node scripts/executeScript.js --openInstallable",
    "hostServer": "node scripts/executeScript.js --hostServer",
    "developerBuild": "node scripts/executeScript.js --developerBuild"
  },
  "repository": "https://github.com/DinoscapeProgramming/Remote-Control",
  "homepage": "https://remote-control-cnp2.onrender.com",
  "keywords": ["remote-control", "remote-desktop", "electron"],
  "author": "DinoscapeProgramming",
  "license": "Apache-2.0",
  "bin": {
    "buildInstallable": "bin/buildInstallable.js",
    "openInstallable": "bin/openInstallable.js",
    "hostServer": "bin/hostServer.js",
    "developerBuild": "bin/developerBuild.js"
  }
}